{% extends 'base.html.twig' %}

{% block title %}Signaler une conversation - VideTonGrenier{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card shadow">
                <div class="card-header bg-warning">
                    <h4 class="mb-0">
                        <i class="fas fa-exclamation-triangle me-2"></i>
                        Signaler une conversation
                    </h4>
                </div>
                <div class="card-body">
                    <div class="alert alert-info">
                        <h6><i class="fas fa-info-circle me-2"></i>Informations sur le signalement</h6>
                        <p class="mb-2"><strong>Annonce :</strong> {{ annonce.titre }}</p>
                        <p class="mb-0"><strong>Utilisateur signalé :</strong> {{ reportedUser.fullName }}</p>
                    </div>
                    
                    <p class="text-muted">
                        Nous prenons très au sérieux la sécurité de notre communauté. 
                        Votre signalement sera examiné par notre équipe de modération.
                    </p>
                    
                    {{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': true}}) }}
                    
                    <div class="mb-3">
                        {{ form_label(form.reason, null, {'label_attr': {'class': 'form-label fw-bold'}}) }}
                        {{ form_widget(form.reason) }}
                        <div class="form-text">
                            Sélectionnez le motif qui correspond le mieux au problème rencontré.
                        </div>
                        {{ form_errors(form.reason) }}
                    </div>
                    
                    <div class="mb-4">
                        {{ form_label(form.description, null, {'label_attr': {'class': 'form-label fw-bold'}}) }}
                        {{ form_widget(form.description) }}
                        <div class="form-text">
                            Ajoutez des détails qui pourraient nous aider à comprendre le problème.
                        </div>
                        {{ form_errors(form.description) }}
                    </div>
                    
                    <div class="d-flex justify-content-between">
                        <a href="{{ path('app_message_conversation', {id: annonce.id}) }}" 
                           class="btn btn-secondary">
                            <i class="fas fa-arrow-left me-1"></i> Retour à la conversation
                        </a>
                        
                        <button type="submit" class="btn btn-warning">
                            <i class="fas fa-exclamation-triangle me-1"></i> Envoyer le signalement
                        </button>
                    </div>
                    
                    {{ form_end(form) }}
                </div>
            </div>
            
            <!-- Conseils de sécurité -->
            <div class="card mt-4">
                <div class="card-header bg-light">
                    <h6 class="mb-0">
                        <i class="fas fa-shield-alt text-success me-2"></i>
                        Conseils de sécurité
                    </h6>
                </div>
                <div class="card-body">
                    <ul class="small mb-0">
                        <li><strong>Ne communiquez jamais d'informations personnelles sensibles</strong> (numéro de carte bancaire, mot de passe, etc.)</li>
                        <li><strong>Privilégiez les rencontres en lieu public</strong> pour les transactions</li>
                        <li><strong>Méfiez-vous des prix anormalement bas</strong> ou des demandes de paiement avant la rencontre</li>
                        <li><strong>Signalez tout comportement suspect</strong> à notre équipe de modération</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}