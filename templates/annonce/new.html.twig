{% extends 'base.html.twig' %}

{% block title %}Déposer une annonce - VideTonGrenier{% endblock %}

{% block meta_description %}Déposez votre annonce gratuitement sur VideTonGrenier. Vendez vos objets ou proposez vos services facilement.{% endblock %}

{% block body %}
<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h3 class="mb-0">📝 Déposer une nouvelle annonce</h3>
                </div>
                <div class="card-body">
                    <div class="alert alert-info">
                        <i class="fas fa-info-circle"></i>
                        <strong>Information :</strong> Votre annonce sera soumise à modération avant d'être publiée sur le site.
                    </div>

                    {{ form_start(form, {'attr': {'class': 'needs-validation'}}) }}
                    
                    {% if form.vars.errors|length > 0 %}
                        <div class="alert alert-danger">
                            <h6>❌ Erreurs dans le formulaire :</h6>
                            {{ form_errors(form) }}
                        </div>
                    {% endif %}
                    
                    <div class="row">
                        <div class="col-md-12 mb-3">
                            {{ form_label(form.titre, null, {'label_attr': {'class': 'form-label fw-bold'}}) }}
                            {{ form_widget(form.titre) }}
                            {{ form_errors(form.titre) }}
                            <div class="form-text">Choisissez un titre accrocheur pour votre annonce (5-255 caractères)</div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            {{ form_label(form.type, null, {'label_attr': {'class': 'form-label fw-bold'}}) }}
                            {{ form_widget(form.type) }}
                            {{ form_errors(form.type) }}
                        </div>
                        <div class="col-md-6 mb-3">
                            {{ form_label(form.categorie, null, {'label_attr': {'class': 'form-label fw-bold'}}) }}
                            {{ form_widget(form.categorie) }}
                            {{ form_errors(form.categorie) }}
                        </div>
                    </div>

                    <div class="mb-3">
                        {{ form_label(form.description, null, {'label_attr': {'class': 'form-label fw-bold'}}) }}
                        {{ form_widget(form.description) }}
                        {{ form_errors(form.description) }}
                        <div class="form-text">Décrivez votre objet ou service en détail (minimum 20 caractères)</div>
                    </div>

                    <div class="row">
                        <div class="col-md-4 mb-3">
                            {{ form_label(form.prix, null, {'label_attr': {'class': 'form-label fw-bold'}}) }}
                            {{ form_widget(form.prix) }}
                            {{ form_errors(form.prix) }}
                            <div class="form-text">Laissez vide pour "Prix à négocier"</div>
                        </div>
                        <div class="col-md-4 mb-3">
                            {{ form_label(form.codePostal, null, {'label_attr': {'class': 'form-label fw-bold'}}) }}
                            {{ form_widget(form.codePostal) }}
                            {{ form_errors(form.codePostal) }}
                        </div>
                        <div class="col-md-4 mb-3">
                            {{ form_label(form.ville, null, {'label_attr': {'class': 'form-label fw-bold'}}) }}
                            {{ form_widget(form.ville) }}
                            {{ form_errors(form.ville) }}
                        </div>
                    </div>

                    <div class="mb-3">
                        {{ form_label(form.localisation, null, {'label_attr': {'class': 'form-label fw-bold'}}) }}
                        {{ form_widget(form.localisation) }}
                        {{ form_errors(form.localisation) }}
                        <div class="form-text">Adresse complète ou point de repère</div>
                    </div>

                    <div class="mb-4">
                        <div class="form-check">
                            {{ form_widget(form.isUrgent) }}
                            {{ form_label(form.isUrgent, null, {'label_attr': {'class': 'form-check-label'}}) }}
                        </div>
                        {{ form_errors(form.isUrgent) }}
                        <div class="form-text">Les annonces urgentes sont mises en avant</div>
                    </div>

                    <div class="alert alert-warning">
                        <i class="fas fa-exclamation-triangle"></i>
                        <strong>À savoir :</strong> 
                        <ul class="mb-0 mt-2">
                            <li>Votre annonce sera vérifiée par nos modérateurs avant publication</li>
                            <li>Vous recevrez une notification dès qu'elle sera validée</li>
                            <li>Les annonces contraires à nos conditions d'utilisation seront refusées</li>
                        </ul>
                    </div>

                    <div class="d-flex justify-content-between">
                        <a href="{{ path('app_annonce_index') }}" class="btn btn-secondary">
                            <i class="fas fa-arrow-left"></i> Retour aux annonces
                        </a>
                        <button type="submit" class="btn btn-primary btn-lg">
                            <i class="fas fa-paper-plane"></i> Soumettre l'annonce
                        </button>
                    </div>

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>

<script>
// Validation côté client
(function() {
    'use strict';
    window.addEventListener('load', function() {
        var forms = document.getElementsByClassName('needs-validation');
        var validation = Array.prototype.filter.call(forms, function(form) {
            form.addEventListener('submit', function(event) {
                if (form.checkValidity() === false) {
                    event.preventDefault();
                    event.stopPropagation();
                }
                form.classList.add('was-validated');
            }, false);
        });
    }, false);
})();
</script>
{% endblock %}