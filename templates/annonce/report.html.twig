{% extends 'base.html.twig' %}

{% block title %}Signaler une annonce - VideTonGrenier{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card shadow">
                <div class="card-header bg-warning">
                    <h4 class="mb-0">
                        <i class="fas fa-exclamation-triangle me-2"></i>
                        Signaler une annonce
                    </h4>
                </div>
                <div class="card-body">
                    <div class="alert alert-info">
                        <h6><i class="fas fa-info-circle me-2"></i>Informations sur l'annonce signalée</h6>
                        <div class="d-flex">
                            {% if annonce.firstImage %}
                                <img src="{{ annonce.firstImage }}" alt="{{ annonce.titre }}" 
                                     class="me-3 rounded" style="width: 80px; height: 80px; object-fit: cover;">
                            {% endif %}
                            <div>
                                <p class="mb-1"><strong>{{ annonce.titre }}</strong></p>
                                <p class="mb-1 small text-muted">Par {{ annonce.user.fullName }}</p>
                                <p class="mb-1 small">{{ annonce.prix ? annonce.prix ~ ' €' : 'Prix non précisé' }}</p>
                                <p class="mb-0 small text-muted">{{ annonce.ville }}, {{ annonce.codePostal }}</p>
                            </div>
                        </div>
                    </div>
                    
                    <p class="text-muted">
                        Nous prenons très au sérieux la qualité et la sécurité des annonces sur notre plateforme. 
                        Votre signalement sera examiné par notre équipe de modération.
                    </p>
                    
                    {{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': true}}) }}
                    
                    <div class="mb-3">
                        {{ form_label(form.reason, null, {'label_attr': {'class': 'form-label fw-bold'}}) }}
                        {{ form_widget(form.reason) }}
                        <div class="form-text">
                            Sélectionnez le motif qui correspond le mieux au problème identifié.
                        </div>
                        {{ form_errors(form.reason) }}
                    </div>
                    
                    <div class="mb-4">
                        {{ form_label(form.description, null, {'label_attr': {'class': 'form-label fw-bold'}}) }}
                        {{ form_widget(form.description) }}
                        <div class="form-text">
                            Décrivez en détail le problème que vous avez identifié avec cette annonce.
                        </div>
                        {{ form_errors(form.description) }}
                    </div>
                    
                    <div class="d-flex justify-content-between">
                        <a href="{{ path('app_annonce_show', {id: annonce.id}) }}" 
                           class="btn btn-secondary">
                            <i class="fas fa-arrow-left me-1"></i> Retour à l'annonce
                        </a>
                        
                        <button type="submit" class="btn btn-warning">
                            <i class="fas fa-exclamation-triangle me-1"></i> Envoyer le signalement
                        </button>
                    </div>
                    
                    {{ form_end(form) }}
                </div>
            </div>
            
            <!-- Types de signalements pour les annonces -->
            <div class="card mt-4">
                <div class="card-header bg-light">
                    <h6 class="mb-0">
                        <i class="fas fa-question-circle text-info me-2"></i>
                        Quand signaler une annonce ?
                    </h6>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6">
                            <h6 class="text-danger">⚠️ Signaler si :</h6>
                            <ul class="small">
                                <li><strong>Contenu inapproprié</strong> : photos ou descriptions choquantes</li>
                                <li><strong>Arnaque suspectée</strong> : prix anormalement bas, demandes suspectes</li>
                                <li><strong>Objet interdit</strong> : vente d'articles prohibés</li>
                                <li><strong>Spam</strong> : annonces répétitives ou publicitaires</li>
                            </ul>
                        </div>
                        <div class="col-md-6">
                            <h6 class="text-success">✅ Ne pas signaler si :</h6>
                            <ul class="small">
                                <li>L'annonce ne vous intéresse pas</li>
                                <li>Le prix vous semble trop élevé</li>
                                <li>Vous n'êtes pas d'accord avec la description</li>
                                <li>Simple désaccord commercial</li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}