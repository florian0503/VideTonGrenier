{% extends 'base.html.twig' %}

{% block title %}Compte suspendu - VideTonGrenier{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card border-danger">
                <div class="card-header bg-danger text-white text-center">
                    <h4 class="mb-0">
                        <i class="fas fa-ban me-2"></i>
                        Compte suspendu
                    </h4>
                </div>
                <div class="card-body text-center">
                    <div class="alert alert-danger">
                        <h5><i class="fas fa-exclamation-triangle me-2"></i>Votre compte a été suspendu</h5>
                        <hr>
                        <p class="mb-0">
                            Votre compte a été suspendu suite à un signalement de notre communauté.
                        </p>
                    </div>
                    
                    {% if user.banReason %}
                        <div class="card mb-4">
                            <div class="card-header">
                                <strong>Motif de la suspension</strong>
                            </div>
                            <div class="card-body">
                                <p class="mb-0">{{ user.banReason }}</p>
                            </div>
                        </div>
                    {% endif %}
                    
                    {% if user.bannedAt %}
                        <p class="text-muted mb-4">
                            <i class="fas fa-calendar me-1"></i>
                            Suspendu le {{ user.bannedAt|date('d/m/Y à H:i') }}
                        </p>
                    {% endif %}
                    
                    <div class="alert alert-info">
                        <h6><i class="fas fa-info-circle me-2"></i>Que faire maintenant ?</h6>
                        <ul class="text-start mb-0">
                            <li>Si vous pensez que cette suspension est injustifiée, contactez notre équipe de support</li>
                            <li>Respectez les règles de notre communauté pour éviter les sanctions futures</li>
                            <li>Les suspensions peuvent être temporaires ou définitives selon la gravité</li>
                        </ul>
                    </div>
                    
                    <div class="d-grid gap-2">
                        <a href="mailto:support@videtongrenier.fr" class="btn btn-primary">
                            <i class="fas fa-envelope me-2"></i>
                            Contacter le support
                        </a>
                        <a href="{{ path('app_logout') }}" class="btn btn-outline-secondary">
                            <i class="fas fa-sign-out-alt me-2"></i>
                            Se déconnecter
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}